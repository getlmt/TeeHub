// Types for AI Try-On feature

/**
 * @typedef {Object} ImageFile
 * @property {string} base64 - Base64 encoded image data (without data URL prefix)
 * @property {string} mimeType - MIME type of the image (e.g., 'image/jpeg', 'image/png')
 */

export const ImageFileType = {};
